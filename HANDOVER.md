# HANDOVER - personal-site

<!-- BEGIN:machine - generated by context/handover.mjs, do not hand-edit -->

Generated: 2026-09-03T04:33:24Z
Branch: docs/handover-profile
Commit: 1ba5830
Upstream: no tracking ref configured (whether these commits are on a remote was NOT checked)
Dirty: 1 file
M HANDOVER.md
Open PRs: none open
Recent: 1ba5830 Regenerate the favicon, Apple icon, and share card in the site's look
7b1f334 Copy audit: drop em-dash titles, repeated bullets, and shouting labels
3b320a4 Tidy the résumé: title line, one-line contact row, stack under each title
48370b7 Redesign: light-first, product-first, generated from the roster
f50aa79 Add Plugrade and How's My Job Fit, fix wrong facts, one title, hiring line
cb85e27 Remove the pre-July site, the token-minting API route, and stale docs
b1b7bd6 Persist theme in localStorage instead of a cookie so routes can be static
058cbaa Curate the project archive into selected and supporting work
5b535da Move conventions into AGENTS.md, add HANDOVER.md and the handover CI check
3fae87e Redesign project pages and link screenshots to live sites (#46)

<!-- END:machine -->

## Next action

Two things only the user can do: enable Vercel Web Analytics in the dashboard
(the package is installed, the API says analytics is not enabled), and request
indexing in Search Console for the pages Google has never fetched. Then confirm
the LinkedIn URL. After that, run `npm run check-links` about once a month so
the "Live, checked" date stays honest.

## State

2026-09-02 (icons): favicon, Apple touch icon, and the Open Graph card are
regenerated with the site's look: a #106b4e tile with a serif T, and a light
share card set in Newsreader. `src/app/fonts/Newsreader-Medium.ttf` is a static
instance (wght 500, opsz 72, made with fonttools) vendored only for these
build-time images; Satori cannot parse the variable TTF. GitHub repo
description, topics, profile bio, and profile location were all updated the
same day (the CLI token now carries the `user` scope).

2026-09-02 (résumé tidy + copy audit): the résumé gained a title line
("Software Developer · Lynnwood, WA (Seattle area) · US citizen"), a one-line
contact row, stack moved under each project title, and a three-line summary.
Résumé-only `stack` strings in the JSON are deliberately short so that line
never wraps. Copy audit against the humanizer pattern list: em dashes removed
from the three long titles (now "Gasolytics", "Amex Roofing", "Throughline",
with `slug` pinned in the JSON so URLs did not change), the repeated
"Source available to read; deployed on Vercel" bullet folded into real
bullets, most "X, so Y" tails rewritten, "actually" dropped, and the
uppercase letter-spaced section labels replaced by plain serif headings.

Portfolio for To Yin Yu, live at www.toyinyu.com. Next.js 16 with Tailwind and
CSS Modules. **The default branch here is `master`, not `main`.**

2026-09-02 (redesign): light-first, product-first. Newsreader and Public Sans
through next/font, tokens in globals.css with a .dark override, theme from
localStorage or the system preference with a toggle in the header. The homepage
and archive are generated from the JSON (featured = full-width screenshot on
the homepage). framer-motion removed; nothing animates in from opacity 0, so
the page reads with JavaScript off. Per the user (2026-09-02): Plugrade is
hidden until it launches (`/projects/plugrade` → 307 `/projects`, off the résumé
too); Amex Roofing and Reachspan are featured with screenshots. Seven featured
products. `scripts/check-links.mjs` verifies every URL and stamps
LINKS_CHECKED_ON in src/lib/site.ts, which feeds the "Live, checked <date>"
line.

2026-09-02 (content pass): Plugrade and How's My Job Fit? are in the roster,
featured, with screenshots, and on the résumé together with Amex Roofing.
all-in-one-URL left the résumé to keep it on one page. Seven wrong facts fixed
(SoloMock WebSocket→WebRTC, Amex Español/22 pages/"every URL indexed",
Gasolytics "51 state and metro", Roadmap 12→13 stages, Gasolytics "daily
pipeline"). One title everywhere: "Software developer". Hiring line and "US
citizen" added to the hero, footer, OG image, and résumé summary. Every
description rewritten; © boilerplate, backticks and "2026" strings removed.
Detail-page meta descriptions are cut at ~155 chars; the sitemap uses each
entry's dateModified. Slugs now strip apostrophes (`hows-my-job-fit`).

2026-09-02 (cleanup): the July curated-archive work is committed and the repo is cleaned.
Every route is now static (theme moved from a cookie to localStorage). The
pre-July site (Hero, About, Skills, Experience, Contact form, voice demo, GitHub
widget, theme toggle) and the public `/api/realtime/session` token-minting route
are deleted; nothing rendered them. Reachspan is hidden from the roster and
`/projects/reachspan` redirects to `/projects`. Upstash, clsx and tailwind-merge
are removed. README.md and docs/STACK.md describe the current site.

`src/lib/projects.data.json` is the single source of truth for the project
roster. The site and the résumé PDF are derived from it (`make resume`). There
is no `PROJECTS_OVERVIEW.md` in this repo.

## Gotchas

Homepage and archive order is the JSON order: featured entries first, then
supporting, hidden last. A featured project needs a 1280×720 screenshot in
public/projects/ and an entry in src/lib/project-visuals.ts or it renders with
no image.

Newsreader is loaded with `weight: "variable"`; next/font refuses `axes` with a
fixed weight list.

Do not run `vercel --prod`. It does not move the production alias.

Vercel Web Analytics is installed in the layout but the Analytics API reports it
as not enabled for the project; enable it in the Vercel dashboard.

## Open decisions

2026-08-11: the LinkedIn contact link is still unresolved and only the user can
supply it.

2026-09-02: the phone number stays on the public résumé PDF until the user says
otherwise.
